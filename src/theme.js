const spacingUnit = 8

export const theme = {
    primaryColor: "#13bb00",
    accentColor: "#e08217",
    backgroundColor: "#ffffff",
    focusedBackgroundColor: "#e4ffe2",
    borderRadius: spacingUnit,
    /**
     * A reference value for all spacing and sizings in pixels
     */
    spacingUnit,
    font: {
        body: {
            fontSize: "1rem"
        }
    }
}
